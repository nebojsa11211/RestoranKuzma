@page "/reserve-table"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the unified tables page which now includes reservation functionality
        Navigation.NavigateTo("/tables", replace: true);
    }
}